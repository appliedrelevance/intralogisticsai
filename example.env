# IntraLogistics Lab Environment Configuration
# Reference: https://github.com/frappe/frappe_docker/blob/main/docs/environment-variables.md

# ================================================================
# CORE CONFIGURATION (Required)
# ================================================================

ERPNEXT_VERSION=v15.64.1
DB_PASSWORD=123

# Custom EpiBus Image Configuration (CRITICAL for deployment)
CUSTOM_IMAGE=frappe-epibus
CUSTOM_TAG=latest
PULL_POLICY=never

# ================================================================
# PLC INTEGRATION (EpiBus specific)
# ================================================================

# PLC Bridge API Configuration (optional - will use guest mode if not set)
FRAPPE_API_KEY=
FRAPPE_API_SECRET=

# PLC Configuration
PLC_POLL_INTERVAL=1.0
PLC_LOG_LEVEL=INFO
FRAPPE_URL=http://frontend:8080

# ================================================================
# EXTERNAL SERVICES (Only if using external database/redis)
# ================================================================

# Only if you use external database (leave empty for containerized MariaDB)
DB_HOST=
DB_PORT=

# Only if you use external Redis (leave empty for containerized Redis)
REDIS_CACHE=
REDIS_QUEUE=

# ================================================================
# OPTIONAL CONFIGURATIONS
# ================================================================

# Container restart policy
RESTART_POLICY=unless-stopped